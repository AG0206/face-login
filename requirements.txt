Django>=5.1.1
opencv-python>=4.12.0.88
face-recognition-models>=0.3.0
# Core packages for facial recognition (optional but recommended):
# dlib>=19.24.1
# face-recognition>=1.3.0
# Alternative face recognition library (requires TensorFlow):
# deepface>=0.0.95

# Installation notes:
# 1. Django and OpenCV are ready to use
# 2. For full face-recognition functionality, install Visual Studio Build Tools
#    and then run: pip install dlib face-recognition
# 3. For an alternative approach, try: pip install deepface